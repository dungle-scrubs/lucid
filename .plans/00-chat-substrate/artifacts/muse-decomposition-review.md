**RANKED FINDINGS - implementation.md only**

**1. Missing milestones the spec demands**
1. `Phase 2 / M2.1+M2.3` - **Spec 3.1 table incomplete.** Descriptor tasks list only argv/streaming/sessionMode/limits but omit 7 load-bearing dimensions the spec requires interpretation + tests for: `storePath` (transcript tail path), `presence/isInteractive`, `contextHook` -> `context` HarnessEvent, `stdin` (pi `< /dev/null`), `provider`, `autonomy`, `tools`, `discoveryFlags`. Risk: M3 tail/spawn and M5 adapter build on wrong paths/flags; Gate 2→3 passes anyway. Fix: Expand M2.1 seams to `contextHook`, `storePath`, `presence`, `stdin`, `provider/autonomy/tools/discoveryFlags`; add RED tests per harness; gate checkbox "all 16 dimensions have descriptor+interpretation test".
2. `Phase 4 / M4.2+M4.3` - **Channel-auth oracle absent.** Spec 4.2/4.7/ D-004 requires refusing impersonated `control {pause|end|switch-path}` and forged frames from cross-user process holding wrong secret; spec defines this as a failure oracle. No milestone owns it (M4.2 tests secret on attach only, M5.1 tests missing secret on store). Risk: attacker sends `end` on stolen epoch and kills conversation; invariants pass. Fix: Add M4.2 RED: `attach` with wrong secret refused, `event/input/control` with stale/wrong secret refused with named `auth-failed`; move `channel-auth` from deferred audit to Gate 4→5.
3. `Phase 4 / M4.2+M5.1` - **Runtime-verified capabilities not wired.** Spec D-008/3.1: `capabilitiesOf(h,model,mode)` with `source: runtime-verified` queried from active harness/registry at adapter attach. M2.3 mentions it for pi descriptor but no milestone implements adapter calling it at `attach` nor degrades to `curated/unknown`. Risk: agent self-asserts vision/streaming, chat sends images to model without it. Fix: Add M5.3 seam: `attach -> capabilitiesOf` query (pi registry, claude statusline probe); test `runtime-verified` vs `curated` fallback; descriptor pin is default not source.
4. `Phase 4 / Backpressure` - **Credit grant path missing.** Spec 4.6: `lucid -> source: credit {tokens}` for droppable class only. M4.3 tests coalescing but no milestone mints/grants `credit` from host/store nor bounds queue depth constant. Risk: reducer tests coalescing in isolation, host never emits credit, adapter overflows. Fix: Add M4.3/M5.1 seam `host.grantCredit()` + constant `DROPPABLE_QUEUE_MAX`; test credit starvation coalesces `token/progress/context` latest-wins, lossless never dropped.

**2. Wrong phase/dependency ordering**
5. `Phase 4 / M4.4 Liveness+state machine` - **Misplaced exactly-once handoff oracle.** M4.4 RED claims to prove `headless<->interactive` mid-flight exactly-once via `resumeFrom+acks` using only reducer+injected clock. Spec 4.7 says this oracle is durable-log ordered exactly-once. Reducer has no seq durability or replay buffer trimming (M5.1 store). Risk: reducer oracle passes, end-to-end still duplicates/drops. Fix: Move mid-flight handoff oracle to M5.4 (requires M5.1+M5.2+M5.3 fake harness both sides); keep M4.4 to heartbeat/lease/epoch fencing only; Gate 4→5 must not claim handoff oracle.
6. `Phase 4 / M4.4 deps: M4.2 only` - **Presence never integrated.** `presence/isInteractive` is defined in normalizer (Phase 2/3) but M4.4/M5 relies on `presence corroborates unattached vs gone`. No dep edge `M4.4 -> M2.1/M3.x` and no store polling task. Risk: Gate 4→5 passes with fake `presence=true` stub, real wire never calls `isInteractive(sid)`. Fix: Change M4.4 deps to `M4.2,M3.1` (fake presence) and add M5.1 task `store polling presence via normalizer` with test "presence alive+heartbeat timeout => interactive-unattached not agent-gone".
7. `Phase 3 / M3.1 -> M2.2` - **Override file not required before execution.** M3.1 depends only on M2.1, so headless runner builds argv before `code defaults + validated override wins; throw with path` (D-006) is proven. Risk: smoke uses code defaults, override merge bug ships to execution. Fix: Add dep `M3.1 -> M2.2` (or Gate 3→4 requires M2.2 green).
8. `Phase 5 / M5.3 deps` - **Adapters scheduled before spike evidence exists.** Assumptions table marks A-002/A-003/A-004 `untested`, yet M5.3 depends on `A-002/A-003/A-004 evidence` and claims `test-first against recorded fixtures from spike`. Fixtures don't exist. Risk: tests tautologically pass on invented JSONL. Fix: Gate 4→5 or M5.3 entry criterion: "SPIKE A-002/A-003 complete and fixtures checked into `spikes/evidence/`"; otherwise block M5.3 and degrade to rungs 2-3 per Escape 2.

**3. Testing/Observability typing**
9. `M4.1 Frame schemas` - **Observability: none but must be required.** Refused frames with named `issue` are the only operator-visible auth/validation signal (spec 4.2). Typed test-first only. Risk: refusals return `refused` without structured `issue` logging, `lucid-v2 log` can't answer "why refused?". Fix: Mark M4.1 `Observability: required` (reducer returns `{verdict, issue}` structured event; host logs it per Architecture).
10. `M5.3 claude ladder` - **Test-first against fixtures insufficient for injection.** Hook `PostToolUse/Stop` injection (~10k cap, harness-mediated, terminal-visible as hook output per A-004 untested) cannot be proven by JSONL tail fixture. Typed test-first + smoke will pass while chunking/visibility broken. Risk: queue never drains in live turn. Fix: Split M5.3: logic (tail parsing, 10k chunking, disposition) test-first on fixtures; injection contract `test-after` with mandatory live-PTY smoke that asserts disposition `applied|queued|rejected` and terminal rendering.
11. `M3.3+M7.2+M6.1` - **Inconsistent typing.** M3.3 and M7.2 real-harness smoke correctly `test-after` (nondeterministic) but M6.1 TUI also `test-after` while Architecture says "TUI reads same structured events as tests" - no assertion that TUI reflects durable log. Fix: Keep M6.1 test-after for rendering, add required assertion `TUI view == fold(conversation log)` (no separate state).

**4. Gates that pass while phase goal unmet**
12. `Gate 2→3` - **Pure + scars != complete.** Checks "interpretation 100% pure" and "four descriptors round-trip v1 scars" but Phase 2 goal is "every v1 scar has owner+test" (16 dimensions). Passes with missing `contextHook/presence/storePath/stdin` etc. Fix: Change to "all descriptor fields in 3.1 table have interpretation function + RED test; `overrides` merge throws with path".
13. `Gate 3→4` - **No runner semantics check.** Checks determinism+smoke+no chat imports but not `stall watchdog only for none-granularity` (M3.1), `send queued mid-turn per A-001` (M3.2), `close drains` . Passes with stub runner. Fix: Add "stall/queue/close REDs green" to gate.
14. `Gate 4→5` - **Claims full oracle list but defers half to M7.1.** Gate says "all four invariants green under full oracle list (4.7)" but channel-auth, spawn-boundary (argvOrder/path traversal/control chars), `resumeLast` race, identity collisions, credit starvation are audited only in M7.1. Risk: invariants green declared early. Fix: Enumerate oracles in gate; check only M4-owned subset or move gate after M7.1.
15. `Gate 1→2` - **Empty suite can be empty.** Lint+typecheck+empty suites green passes even if module-comments-per-Boundaries missing and Bun lane not run separately. Fix: Add "src/* has module comment stating ownership; `bun test` lane executed".

**5. Escape hatches that do not work**
16. `Escape 4` - **Rewrite oracle != fix invariant.** "If mid-flight handoff oracle cannot pass, narrow to quiesced-only and rewrite oracle - never deleted." Violates governing constraint "fully tested ... not happy-path demos" and D-002 epoch fencing purpose (lease-expiry aborts in-flight turn). Rewriting oracle hides exactly-once failure, lets two writers briefly apply input. Fix: Delete hatch; require oracle to pass; if truly impossible, new RFC decision amending spec, not silent rewrite.
17. `Escape 5` - **Contradicts Gate 1→2 and D-005.** Proposes shipping Node-first if Bun/Node duality blocks execution, but D-005 requires both via injected primitives and Gate 1→2 requires Node AND Bun green. No waiver path. Fix: Add gate waiver clause or change hatch to "execution layer blocked => whole Phase 3 blocked, bump decision D-005 via iterate mode".
18. `Escape 1` - **Incomplete cut.** Says if A-001 fails cut headless-session mode, `openSession` pi-only. But M3.2 already built claude session runner and Gate 3→4 smokes it. No rollback plan. Fix: Condition M3.2 on A-001 pass; if fail, M3.2 skipped, descriptor `session:false` for claude, Gate 3→4 smoke adjusted.

**6. Contradictions with ledger or spike evidence**
19. `M2.1 / decodeIdentity + spike A-001` - **Identity re-emitted per turn.** Evidence A-001: `system/init` with same `session_id` emitted at start of EVERY turn, not once-per-session. M2.1 `decodeIdentity` and M3.1 `emit identity` assume once-per-process. Risk: duplicate `identity` HarnessEvents per turn confuse capability/seq mapping. Fix: Note in M2.1/M3.1: treat `system/init` as `turn-start` metadata, dedupe per turn, emit `identity` only on first turn or when id changes; add RED with A-001 raw ndjson.
20. `M5.3 / presence gate + spike A-005` - **Harness offers zero writer guard (D-018).** A-005 proves concurrent `claude -p --resume <live-id>` appends into live session with no lock, even headless-headless. Implementation's "headless takeover REFUSED while presence holds" only guards interactive presence, not two headless contenders both seeing `presence=false`. Risk: two headless `openSession(--resume)` race from different hosts both succeed. Fix: Document that presence gate is necessary not sufficient; rely on epoch fencing + secret check to refuse stale epoch; add forbidden-path test for headless-headless concurrent resume (both via gate) expecting one `refused {stale-epoch}`.
21. `Landing / M7.3 vs D-014` - **Per-phase PR vs end merge.** D-014: "phase branches off main; one PR per phase merged after phase gate; cross-family review per PR". M7.3 says "Final phase PRs merged to main in both repos". Risk: teams batch PRs, skip per-phase codex/muse-spark review. Fix: Change M7.3 to "each phase PR merged immediately after its gate; M7.3 only tags".

