{
  "name": "lucid-v2",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "biome check . && tsc --noEmit && bun test",
    "test": "bun test",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.7",
    "@types/bun": "^1.3.14",
    "lefthook": "^2.1.10",
    "typescript": "^7.0.2"
  }
}
