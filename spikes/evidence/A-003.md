# A-003 - Bare-session tiers: DEFERRED (retryable; not a milestone-1 blocker)

Date: 2026-08-10 · claude CLI 2.1.226 · macOS (pro) · model: sonnet
Sandbox: `spikes/a003-bare/`

## Verdict

DEFERRED. The isolated re-run started a bare session (pid 73991) but the
driver TERM'd it at `startup-prompt-timeout` before it reached a usable
prompt, so zero transcript arrivals were captured
(`transcript-observation/arrivals.ndjson` empty). This is a driver startup-
timing issue on a truly bare dir (no `.claude`, `--setting-sources project`
selects nothing), not evidence against the assumption. Deferred rather than
failed: rungs 2-3 are the FALLBACK tiers, and M5.3's entry criterion already
requires A-003 evidence before those rungs are built, so deferral is safe
and self-gating.

## Retry recipe (for whoever resumes)

- Widen the driver's post-spawn settle from ~10s and gate the first `say` on
  a real readiness marker in the pty (e.g. the prompt box), not a fixed sleep.
- Snapshot transcript slug-dir file list BEFORE spawn (already done:
  `transcripts-before.txt`) so the watcher can pick the genuinely new file.
- A bare dir needs no isolation flag for hooks (there are none), but the
  machine-global Stop hooks still hang it - keep `--setting-sources project`
  (D-025) or `HERDR_ENV` unset even for the "bare" test on THIS machine.

## Original BLOCKED note (Codex run, superseded by the retry above)

No A-003 Claude session was started after the A-002 startup and
global-hook isolation work reached its three-attempt boundary.

## Observations

- `spikes/a003-bare/` remained bare for this measurement: it has no `.claude`
  configuration directory.
- The prepared driver and watcher were updated only inside the throwaway
  sandbox to capture timestamped raw JSONL arrivals and exact spawned PIDs.
- **Not measured:** incremental transcript append before visible turn
  completion, arrival lag, and delivery of `COOP-DELIVERED` after `cat
  queue.txt`.
- No A-003 process PID exists because this experiment was not launched.

## Consequences for the plan

- Neither observe-only transcript tailing nor cooperative polling has a live
  validation result on Claude Code 2.1.226. Keep rungs 2-3 unclaimed.
- A retry should begin directly with the now-known interactive startup handling
  and use a new baseline of transcript file paths before the session starts.
