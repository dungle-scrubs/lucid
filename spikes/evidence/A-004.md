# A-004 - Constraint characterization: DEFERRED (no architectural impact)

Date: 2026-08-10 · claude CLI 2.1.226 · macOS (pro) · model: sonnet
Sandbox: `spikes/a002-hooks/`

## Verdict

DEFERRED. A-002 now has a clean isolated session (see A-002.md), but this
run did not script the three A-004 measurements (30k-char reason cap,
mid-session config edit, injected-text rendering). Deferred rather than
blocked: per its own impact line, A-004 affects only chunking limits,
adapter wording, and skill guidance - NO architectural impact. It can be
measured any time the hooks adapter is next exercised (M5.3).

## What is already known (from A-002's clean run)

- Injected feedback renders in the terminal as hook output, not a chat
  bubble (visible as the `HUMAN FEEDBACK` / "Stop hook error: HUMAN
  FEEDBACK (turn end)" lines in the earlier pty capture). The exact 10k cap
  and the mid-session-capture behavior remain to be measured.

## Original BLOCKED note (superseded)

Depends on a clean A-002 interactive hook session; none ran after the
bounded A-002 preflight exhausted its allowed startup attempts.

## Observations

- **Feedback size cap:** not measured. No 30,000-character hook reason with
  start/end markers was delivered to a live model.
- **Hooks captured at session start:** not measured. No project hook setting
  was edited mid-session and no later tool call was observed.
- **Terminal rendering:** the preflight PTY does demonstrate normal interactive
  terminal bytes, but it contains no injected hook feedback. It cannot support
  the required rendering conclusion. See
  `spikes/a002-hooks/setting-sources-pty.log`.
- The project hook configuration was not changed outside the throwaway sandbox,
  and no user-level configuration was modified.

## Consequences for the plan

- Do not set chunking limits, state hook-capture semantics, or prescribe the
  user-visible rendering of injected feedback from this run.
- Re-run A-004 only after A-002 has one independently bounded successful
  interactive session using project-only settings.
