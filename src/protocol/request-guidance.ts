/** Request-scoped model guidance, not a harness permission or sandbox. */
export const LUCID_REQUEST_GUIDANCE =
  "This request comes from Lucid. Read and inspect files as needed, but only create or modify the Lucid artifact. Do not change project files or implement code changes. These restrictions apply to this Lucid request, not to work requested separately in the terminal.";
