# Interactive hook recording

`session-start.log` is an unchanged SessionStart hook recording used by
`test/modes/interactive.test.ts` to verify attach-intent parsing. It came
from the original hook experiment and remains a compatibility fixture.
Capture new evidence through a harness; never hand-edit this recording.
